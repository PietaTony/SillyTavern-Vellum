/**
 * 「怎麼更新」的文案正本 —— 只有一份（橫幅與設定頁共用）。
 *
 * 🔴 **這裡刻意沒有可複製的指令**（2026-08-27，移除 Docker 之後）。
 * 上一版是 `UPDATE_COMMAND = 'docker compose pull && docker compose up -d'`，
 * 顯示在畫面上、旁邊還有一顆「複製指令」。Docker 方案移除之後那條指令**永遠跑不起來**。
 *
 * 🔴 **而且不可以換成另一條指令。** 走 zip 的使用者手上沒有 git、沒有 pnpm ——
 * 「下載新版 → 解壓 → 保留 `data/` → 重啟」**沒有任何單行指令做得到**。
 * 換一條指令 ＝ 換一個新的謊。⇒ 改成「開 Releases 頁 ＋ 三行步驟」。
 *
 * ⚠️ 舊檔名 `copyCommand.ts` 與「可複製指令」這個概念一起廢掉了，不要復活它。
 */

/** 發佈頁。🔴 版本在這裡下載，不在別的地方。 */
export const RELEASES_URL = 'https://github.com/PietaTony/SillyTavern-Vellum/releases';

/**
 * 更新的三個步驟。
 * 🔴 **第 2 步是全部的重點**：`data/` 是使用者的角色、對話、世界書與金鑰。
 * 忘了搬就是「更新完發現什麼都不見了」，而畫面會顯示成正常的空狀態 —— 看不出是災難。
 */
/** ⚠️ **純文字，不要寫 markdown 記號** —— 這幾行是直接丟進 `<Typography>` 的。 */
export const UPDATE_STEPS: readonly string[] = [
  '到 Releases 頁下載最新版的 zip，解壓到一個新的資料夾。',
  '把舊資料夾裡的 data 資料夾整個複製過去 —— 你的角色、對話與設定都在裡面。',
  '開新資料夾裡的啟動檔；確定沒問題之後才刪掉舊資料夾。',
];

/** 為什麼不是按一下就更新完。🔴 說清楚，不要讓人覺得只是懶得做。 */
export const UPDATE_WHY = `為什麼不是按一下就更新完：這個 app 就是你電腦上的一個資料夾，
它沒辦法在執行中把自己換掉，也不該有權限刪你的檔案。
另一個理由：更新前你應該先看過這一版改了什麼 —— 尤其是標著破壞性變更的那幾版。`;
