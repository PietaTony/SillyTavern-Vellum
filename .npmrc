# 釘死 Node 版本 —— 版本漂移造成的「在我這邊可以跑」對 AI agent 無法診斷。
#
# 為什麼是 24.x：jsdom 30 的 engines 是 ^22.22.2 || ^24.15.0 || >=26.0.0，
# 而系統 Node 是 20.19.5（缺 worker_threads.markAsUncloneable）⇒ vitest 的 jsdom 環境起不來。
# 實測依據：node -p "require('jsdom/package.json').engines"（2026-08-25）
use-node-version=24.15.0
