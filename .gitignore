node_modules/
dist/
dist-server/
coverage/
.DS_Store
*.local
.env
.env.*
!.env.example
# TanStack Router 產生的路由樹
src/app/routeTree.gen.ts
# Playwright
test-results/
playwright-report/
# 🔴 本機資料：角色卡、對話、金鑰。永遠不進版控
data/
# 本機資料庫（現在沒有，但後端重寫可能會用上）
*.db
*.db-journal
*.sqlite
*.sqlite3
data.bak-*

# TanStack Router 的建置暫存 —— 產生檔，不進版控
.tanstack/
dist-zip/
dist-app/
electron/updater.bundle.cjs

# 中控線派工用的 git worktree，harness 建在 repo 內部（不是外面）。
# 每個 worktree 都帶自己的一份 biome.json，`biome check .` 從 repo 根往下遞迴
# 撞到它會回「Found a nested root configuration」而整支 pnpm lint 變紅——
# 而且這個目錄本來就不該進版控（那是另一個 agent 的暫時工作樹，不是這個 repo 的內容）。
.claude/worktrees/
