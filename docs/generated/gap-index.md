<!-- AUTO-GENERATED by scripts/gen-doc-index.ts — do not edit. Run: pnpm gen:doc-index -->

# GAP index（generated）

> 正本：`.claude/agents/<agent>.md` §4。本檔只供搜尋；**以 agent 檔為準**。

| ID | Agent | Trap | Source |
|---|---|---|---|
| GAP-3 | providers | Gemini's `includeThoughts: true` returned **no thought part in six separate real calls** — and billed the thinking tokens anyway | `GAP-3` |
| GAP-37 | chat-core | No history truncation. Past the context window the vendor returns 400 and the room is **permanently stuck** | `GAP-37` |
| GAP-40 | providers | Export must exclude `secrets.json`. The vendor count went from 1 to 26 — one careless `data/` archive leaks 26 billing-attached keys | `GAP-40` |
| GAP-49 | chat-core | Chat import keeps `{id,role,text,at}` only. **Existing swipes vanish silently** | `GAP-49` |
| GAP-50 | prompt-assembly | `promptWorld.ts`'s header comment was stale and actively misleading, and misled two separate reviews. **A wrong comment costs more than no comment** | `GAP-50` |
| GAP-53 | worldbook | The UI offers 8 positions; four of those buckets have **zero consumers on the server**. Pick one and your text silently never reaches the prompt | `GAP-53` |
| GAP-54 | chat-core | A dropped stream has no catch — `busy` stays true and the composer locks. The "retry" button resends nothing, it only clears the banner | `GAP-54` |
| GAP-55 | providers | First-run blocked Anthropic on the strength of **one stale comment** ("/test only reaches Google"). The backend had supported it for a long time | `GAP-55` |
| GAP-56 | worldbook | The `NOT_ALL` control is labelled with `NOT_ANY`'s meaning. The engine's own comment warns the name lies — the UI repeated the lie anyway | `GAP-56` |
| GAP-58 | platform | A 32 MB upload limit that **never once applied** — an 8 MB limit registered first and Hono runs them in order | `GAP-58` |
| GAP-66 | characters | Edits were written to `characters/<id>.json` but export rebuilt from the PNG — **edited characters exported as if never edited** | `GAP-66` |
| GAP-68 | characters | Half a card persisted after a reload — `avatar`/`greetings` survived but `name` did not, so finishing the form attached another card's alternates to a new character | `GAP-68` |
| GAP-70 | characters | `personaId` could be PATCHed to an id that does not exist. It returned 200 | `GAP-70` |
| GAP-74 | characters | `card.ts:7` says `chara` is "V2-compatible". It is not. Only cards *without* a `spec` field are downgraded; anything with `spec` is stored as-is. **Check `spec` before parsing** | `GAP-74` |
| GAP-78 | extensions | **A domain allowlist does not make an installer safe.** Anyone can publish to a popular CDN; the allowlist only blocks the obscure sources | `GAP-78` |
| GAP-81 | card-scripts | There is **no CSP anywhere in this repo**. The sandbox stops the card reading the page; it does not stop the card sending what it can already reach | `GAP-81` |
| GAP-83 | card-scripts | A card's iframe can `location.href` itself to any URL and take the data with it. **Neither CSP inside the frame nor the sandbox attribute stops it** — only the host page's `frame-src` does | `GAP-83` |
| GAP-88 | audio | A track list that **replaces** and one that **appends** are different operations. One function doing both is how data gets silently dropped | `GAP-88` in this repo |
| GAP-88 | presets | **Partial update and full replace must be different functions.** One "set" that sometimes merges and sometimes overwrites is how variables were silently lost before | `GAP-88` / `GAP-123` in this repo |
| GAP-91 | chat-core | Out-of-range swipe index is silently clamped, so asking for candidate 999 returns the last one and nobody says anything | `GAP-91` |
| GAP-98 | platform | `electron-builder` publishes to GitHub Releases on its own, and signs with whatever certificate it finds in the local keychain. It found a **company** one | `GAP-98` / remove-docker report ④ |
| GAP-100 | platform | `identity: null` does not produce "unsigned", it produces **"damaged"** — Gatekeeper rejects it outright instead of showing the unidentified-developer prompt | `GAP-100` |
| GAP-105 | platform | Two bugs covering for each other: the desktop app's port was hard-coded and the startup error had no handler, but the health check reached *someone* (a dev server) and it all looked like success | `GAP-105` |
| GAP-113 | platform | The verification scanned more than the action changed. `main` moved forward with **no matching Release** and a version number permanently skipped | `GAP-113` |
| GAP-114 | platform | The release-notes gate caught "template" and "blank" but not "last version's text". v0.2.5 shipped v0.2.4's notes | `GAP-114` |
| GAP-119 | chat-core | Swipe lore recompute compared *stripped* text against *raw* text — always false. The fixtures used the wrong unit too, so 7 tests were green about a world that did not exist | `GAP-119` |
| GAP-120 | worldbook | Applying a world is **cumulative, not a switch**. Moving from greeting A to greeting B leaves A's entries on (9 grew to 25) | `GAP-120` |
| GAP-123 | presets | **Partial update and full replace must be different functions.** One "set" that sometimes merges and sometimes overwrites is how variables were silently lost before | `GAP-88` / `GAP-123` in this repo |
